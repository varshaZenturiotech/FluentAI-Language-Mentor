services:
  - type: static
    name: fluentai-language-mentor
    env: static
    rootDir: ui
    buildCommand: npm install && npm run build
    publishPath: dist
    routes:
      # Rewrite all nested routes to index.html for client-side routing (React Router)
      - type: rewrite
        source: /*
        destination: /index.html
